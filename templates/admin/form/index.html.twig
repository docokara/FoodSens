{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    {{ include('base/nav.html.twig') }}
 <div>
<table>
<thead>
<tr>
{% if data is empty %}
Vide !
{% else %}
{% for key in data[0]|keys %}
  <th>{{ key }}</th>
   {% endfor %}
   </tr>
   </thead>
   <tbody>
  {% for element in data %}
    <tr>
      {% for info in element %}
      
        {% if info is iterable %}
        <td>
            {% for el in info %}
              {{el}}
            {% endfor %}
        </td>
        {% else %}
          <td>{{info}}</td>
        {% endif %}
    {% endfor %}
     <td> <a href="{{ path('admin_edit', {"id": element.id,"name" :   name }) }}">edit</a></td>
      <td> <a href="{{ path('admin_delete', {"id": element.id,"name" : name}) }}">delete</a></td> 
  {% endfor %}
    </tr>
    {% endif %}
  </tbody>
  </table>
  <a href="{{ path('admin_create',{"name" :name }) }}">Create</a> 
</div>

{% endblock %}
